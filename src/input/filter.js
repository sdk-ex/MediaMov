// filter.js : This file uses the src/data packages and
// regex plus a few others to account for errors within 
// the users input. For more information check the docs.

const string = require();
const error = require();

class filter {

    constructor() {

    }
}